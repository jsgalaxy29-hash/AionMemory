@using Aion.AppHost.Services.Rendering
@using Microsoft.AspNetCore.Components.Forms

<InputNumber TValue="long?" class="input" data-field-renderer="number" Value="@CurrentValue" ValueChanged="OnValueChanged" ValueExpression="() => CurrentValue" disabled="@Disabled" />

@code {
    [Parameter, EditorRequired] public FormFieldRenderModel Field { get; set; } = default!;
    [Parameter] public object? Value { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public EventCallback<object?> ValueChanged { get; set; }

    private long? CurrentValue => FieldValueConverters.ToLongValue(Value);

    private Task OnValueChanged(long? value) => ValueChanged.InvokeAsync(value);
}
